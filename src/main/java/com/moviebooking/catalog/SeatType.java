package com.moviebooking.catalog;

/**
 * Physical seat tiers. Prices are configured per show and per seat type;
 * weekend pricing is applied on top as a time-based surcharge.
 */
public enum SeatType {
    REGULAR,
    PREMIUM,
    RECLINER
}
