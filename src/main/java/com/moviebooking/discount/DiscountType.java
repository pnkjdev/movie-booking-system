package com.moviebooking.discount;

public enum DiscountType {
    /** value = percentage off the subtotal, optionally capped. */
    PERCENT,
    /** value = flat amount off the subtotal. */
    FLAT
}
